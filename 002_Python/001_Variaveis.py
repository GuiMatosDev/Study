# Sintaxe padrão para armanezar variáveis
variavel = input('Digite seu nome: ')
# Input - entrada de dados, permite o usuário digitar

# Tipos primitivos de Dados
string = str(input('Digite seu nome: '))
inteiro = int(input('Digite um numero: '))
fracionado = float(input('Digite sua altura: '))
booleano = bool('True para verdadeiro, False para Falso: ')

# Caso não declarar automaticamente vira uma string
